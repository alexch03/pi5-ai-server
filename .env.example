# Copy to .env and fill in. Used by client/ scripts that talk to the Pi over LAN.
PI_HOST=raspberrypi.local
PI_USER=pi
PI_PASSWORD=changeme
# Ollama API endpoint exposed by the Pi (see install/03_install_ollama.sh)
OLLAMA_HOST=http://raspberrypi.local:11434
